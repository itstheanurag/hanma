name: tooling-prettier
category: feature
featureType: tooling
description: Prettier for formatting
framework: express

includes:
  - snippet: tooling/prettier/config.hbs
    path: .prettierrc

devDependencies:
  - prettier

scripts:
  format: "prettier --write ."
