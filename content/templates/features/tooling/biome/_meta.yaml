name: tooling-biome
category: feature
featureType: tooling
description: Biome for linting and formatting

includes:
  - snippet: tooling/biome/config.hbs
    path: biome.json

devDependencies:
  - "@biomejs/biome"

scripts:
  lint: "biome check ."
  format: "biome format . --write"
